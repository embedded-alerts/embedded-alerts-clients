# embedded-alerts-clients

Generated and hand-written Embedded Alerts clients for supported application runtimes.

Initialized through `DEN-1949` as a testable `clients` foundation. Product behavior continues through focused pull requests.

```bash
python3 scripts/verify_repo.py
```
